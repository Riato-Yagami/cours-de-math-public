\def\a{\ensuremath{\textcolor{Red}{a}}}
\def\b{\ensuremath{\textcolor{Blue}{b}}}
\def\q{\ensuremath{\textcolor{Green}{q}}}

\df{}{
    Soient \a{} et \b{} deux nombres, avec \b{} non nul.
    \begin{itemize}
        \item On appelle \key{quotient} de \a{} par \b{} le nombre \q{} tel que $\b \times \q = \a$.
        \item On peut écrire le quotient sous forme \key{fractionnaire} : $\dfrac{\a}{\b}$.
        \item Si \a{} et \b{} sont des entiers, on dit que $\dfrac{\a}{\b}$ est une \key{fraction}.
    \end{itemize}
}[\wiki{Quotient}]